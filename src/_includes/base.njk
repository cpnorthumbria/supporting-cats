<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{ title }}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">

    <style>
      html {
        scroll-behavior: smooth;
      }

      #toTopBtn {
        position: fixed;
        bottom: 20px;
        right: 20px;
        display: none;
        z-index: 1030;
      }

      /* Add margin/padding to the bottom of nested lists only */
      ol ul,
      ol ol,
      ul ul,
      ul ol {
        margin-bottom: 1rem;
      }

      /* === Custom theme overrides === */
      :root {
        --custom-purple: #7a1b72;
      }

      /* Header background */
      header.bg-dark,
      .navbar.bg-dark,
      .bg-dark {
        background-color: var(--custom-purple) !important;
      }

      /* Navbar links */
      .navbar-dark .navbar-nav .nav-link {
        color: #fff;
      }
      .navbar-dark .navbar-nav .nav-link.active,
      .navbar-dark .navbar-nav .nav-link:focus,
      .navbar-dark .navbar-nav .nav-link:hover {
        color: #ffd6fa; /* lighter hover shade */
      }

      /* For elements using .text-bg-dark */
      .text-bg-dark {
        background-color: var(--custom-purple) !important;
        color: #fff !important;
      }

    /* === Index page buttons === */
      .btn-outline-dark {
        background-color: var(--custom-purple) !important;
        color: #fff !important;
        border: 1px solid var(--custom-purple) !important;
        padding: 1.5rem !important;
        font-weight: 500;
      }

      .btn-outline-dark:hover,
      .btn-outline-dark:focus {
        background-color: #ffd6fa !important;
        color: var(--custom-purple) !important;
        border: 1px solid var(--custom-purple) !important;
      }

      /* Title and general accents */
      h1, h2, h3, .display-6 {
        color: var(--custom-purple);
      }

      /* Remove card border completely */
      .card {
        border: none !important;
        border-radius: 0.25rem !important; /* use 0 for square corners */
        overflow: hidden;
      }

      /* Purple header bar */
      .card-header.text-bg-dark {
        background-color: var(--custom-purple) !important;
        border: none !important;
        border-radius: 0 !important;
        color: #fff !important;
      }

      /* Remove list-group borders entirely */
      .list-group {
        border: none !important;
      }

      /* Individual link lines only at the bottom */
      .list-group-item {
        border: none !important;
        border-bottom: 1px solid var(--custom-purple) !important;
        border-radius: 0 !important;
      }

      /* List-group link colours */
      .list-group-item-action {
        color: var(--custom-purple) !important;
      }

      /* Hover/focus background for links */
      .list-group-item-action:hover,
      .list-group-item-action:focus {
        background-color: #f6e9f5 !important; /* very light tint of #7a1b72 */
        color: var(--custom-purple) !important;
      }

      /* Make sure header and first link join cleanly */
      .card-header + .list-group .list-group-item:first-child {
        border-top: none !important;
      }

      /* Keep bottom edges square, no extra rounding */
      .list-group-item:last-child {
        border-bottom-left-radius: 0 !important;
        border-bottom-right-radius: 0 !important;
      }
    </style>


</head>
  <body id="top">

  {% include 'header.njk' %}

    <main class="mb-5">
      <div class="container my-5">
        {{ content | safe }}
    </main>
  
  
  <a href="#top" 
    class="btn btn-light btn-lg rounded-circle" 
    id="toTopBtn" 
    title="Back to top">
    <i class="bi bi-arrow-up fs-6"></i>
  </a>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
  <script>
    const toTopBtn = document.getElementById("toTopBtn");

    window.addEventListener("scroll", () => {
      if (document.documentElement.scrollTop > 200) {
        toTopBtn.style.display = "block";
      } else {
        toTopBtn.style.display = "none";
      }
    });
  </script>

  </body>
</html>